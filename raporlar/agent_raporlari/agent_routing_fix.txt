You are an expert Python engineer. The Multi-Agent Orchestration System has 3 criticalbugs that must be fixed immediately. Read everything carefully before touching any file.═══════════════════════════════════════════════════════════                  MEVCUT SORUNLAR═══════════════════════════════════════════════════════════SORUN 1: Planner, kod yazma görevlerini Coder'a değil Researcher'a atıyor.         Sonuç: Gerçek kod yazılmıyor, sadece açıklama metni üretiliyor.SORUN 2: Executor "rapor_olusturma" görevini 3 denemede tamamlayamıyor.         Sonuç: Dosya workspace'e kaydedilmiyor.SORUN 3: duckduckgo_search kütüphanesi artık "ddgs" olarak yeniden adlandırıldı.         Sonuç: Her aramada RuntimeWarning çıkıyor.═══════════════════════════════════════════════════════════              DÜZELTİLECEK DOSYALAR VE SIRALAMA═══════════════════════════════════════════════════════════Kesinlikle bu sırayı takip et:  Adım 1: Terminal komutu — kütüphane güncelle  Adım 2: tools/web_search.py — import güncelle  Adım 3: agents/planner_agent.py — sistem prompt güncelle  Adım 4: agents/executor_agent.py — dosya kaydetme düzelt  Adım 5: Test et━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ADIM 1: TERMINAL — KÜTÜPHANEYİ GÜNCELLE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━Terminalde şu komutları sırasıyla çalıştır:  pip uninstall duckduckgo-search -y  pip install ddgsBitti mi? Bir sonraki adıma geç.━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ADIM 2: tools/web_search.py — IMPORT GÜNCELLE━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Dosyayı aç. Şu satırı bul:  from duckduckgo_search import DDGSŞununla değiştir:  from ddgs import DDGSBaşka hiçbir şeye dokunma. Kaydet.━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ADIM 3: agents/planner_agent.py — KRİTİK DÜZELTME━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━PROBLEM:Planner şu an kod yazma görevlerini "researcher" tipinde üretiyor.Bu yüzden Coder Agent hiç devreye girmiyor, sadece araştırma metni çıkıyor.YAPILACAK:Planner Agent'ın system_prompt'unu bul.İçinde "agent_type" veya "assigned_to" veya görev dağılımını anlatan kısım var.O kısmı aşağıdaki kurallara göre güncelle:Mevcut system_prompt'un SONUNA şu bloğu ekle(mevcut promptu silme, sadece en sona ekle):------------------------------------------------------KRİTİK GÖREV DAĞITIM KURALLARI — ASLA İHLAL ETME:Görev tipine göre hangi ajana atanacağını belirle:  "researcher" → SADECE şu görevler için kullan:    - İnternet araştırması gerektiren görevler    - Kütüphane veya API dokümantasyonu araştırma    - Kavram veya yöntem araştırma    - ASLA kod yazma görevi verme  "coder" → MUTLAKA şu görevler için kullan:    - Python scripti yazma    - Fonksiyon veya sınıf yazma    - Test dosyası yazma (unittest, pytest)    - Mevcut kodu düzenleme veya refactor etme    - requirements.txt veya config dosyası oluşturma  "executor" → SADECE şu görevler için kullan:    - Yazılan kodu çalıştırma ve test etme
    - Dosya veya klasör oluşturma/silme    - Shell komutu çalıştırma    - Sonuçları dosyaya kaydetme  "critic" → Her coder çıktısından sonra otomatik devreye girer.             Planner bunu ayrıca planlamak zorunda değil.ÖRNEK DOĞRU PLAN:  Görev: "CSV birleştiren Python scripti yaz"    [    {      "id": "arastirma",      "agent_type": "researcher",      "description": "pandas kütüphanesi ile CSV birleştirme yöntemlerini araştır"    },    {      "id": "kod_yazimi",      "agent_type": "coder",      "description": "CSV dosyalarını birleştiren ve istatistik hesaplayan Python scripti yaz",      "depends_on": ["arastirma"]    },    {      "id": "test_yazimi",      "agent_type": "coder",      "description": "Yazılan script için pytest ile unit testler yaz",      "depends_on": ["kod_yazimi"]    },    {      "id": "calistir",      "agent_type": "executor",      "description": "Scripti çalıştır ve sonuçları workspace'e kaydet",      "depends_on": ["test_yazimi"]    }  ]UNUTMA: Kod yazan görevler MUTLAKA "coder" tipinde olmalı.        "researcher" asla kod yazmaz, sadece araştırır.------------------------------------------------------━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ADIM 4: agents/executor_agent.py — DOSYA KAYDETME DÜZELTMESİ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━PROBLEM:Executor "rapor_olusturma" görevini 3 denemede tamamlayamıyor.
Büyük ihtimalle LLM'den gelen kod bloğunu parse edemiyorveya workspace path'ini bulamıyor.YAPILACAK:Executor Agent'ın kod çalıştırma ve dosya kaydetme fonksiyonunu bul.Şu iyileştirmeleri uygula:1. LLM çıktısından kod bloğunu parse eden fonksiyonu güçlendir:  import re  def extract_code_from_response(self, response: str) -> str:      """LLM cevabından kod bloğunu çıkar — her formatta çalışır"""            # Önce ```python ... ``` bloğunu dene      pattern = r"```python\s*(.*?)\s*```"      match = re.search(pattern, response, re.DOTALL)      if match:          return match.group(1).strip()            # Sonra ``` ... ``` bloğunu dene      pattern = r"```\s*(.*?)\s*```"      match = re.search(pattern, response, re.DOTALL)      if match:          return match.group(1).strip()            # Hiç ``` yoksa — tüm cevabı kod olarak kabul et      # Ama "import" veya "def" veya "class" içeriyorsa      if any(keyword in response for keyword in ["import ", "def ", "class ", "print("]):          return response.strip()            # Hiçbiri değilse boş döndür      return ""2. Dosya kaydetme işlemini güçlendir:  async def save_code_to_file(      self,      code: str,      filename: str,      project_slug: str  ) -> dict:      """Kodu workspace/projects/{slug}/src/ içine kaydet"""            from pathlib import Path      
      if not code or not code.strip():          return {"success": False, "error": "Kaydedilecek kod boş!"}            # Path oluştur      project_root = Path("workspace") / "projects" / project_slug      src_dir = project_root / "src"            # Klasörü garantile      src_dir.mkdir(parents=True, exist_ok=True)            # Dosya adını temizle      if not filename.endswith(".py"):          filename = filename + ".py"            target = src_dir / filename            try:          target.write_text(code, encoding="utf-8")          return {              "success": True,              "path": str(target),              "lines": len(code.splitlines()),              "size": len(code.encode("utf-8"))          }      except Exception as e:          return {"success": False, "error": str(e)}3. Executor'ın ana görev işleme döngüsünde şu kontrolü ekle:  # Görev başında proje yapısını garantile  from tools.file_manager import ensure_project_structure  ensure_project_structure(project_slug)    # LLM cevabından kodu çıkar  code = self.extract_code_from_response(llm_response)    if not code:      # Kod bulunamadı — görevi başarısız say ve logla      return TaskResult(          success=False,          error="LLM cevabında çalıştırılabilir kod bulunamadı",          fix_hint="CODE_EXTRACTION_FAILED: LLM sadece metin döndürdü, kod üretmedi"      )4. Executor'ın system_prompt'una şunu ekle:
  """  Sen bir kod yürütücüsüsün. Görevin sadece şunlar:  1. Verilen Python kodunu workspace/projects/ içine kaydet  2. Kodu çalıştır ve sonucu raporla  3. ASLA kendin kod yazma — sadece ver  4. Dosya kaydedemezsen hata mesajını açıkça belirt  Yanıtın şu formatta olmalı:  {"action": "save_and_run", "filename": "dosya.py", "code": "...kod..."}  """━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ADIM 5: BONUS — CODER AGENT ÇIKTI FORMATI━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━Coder Agent'ın system_prompt'una şunu ekle:  """  ÇIKTI FORMATI — ASLA DEĞİŞTİRME:    Yazdığın kodu MUTLAKA şu formatta döndür:    {"filename": "dosya_adi.py", "code": "...kodun tamamı buraya..."}    YASAK:  - Kod dışında açıklama yazma  - Markdown başlık yazma (## gibi)  - "İşte kod:" gibi giriş cümleleri yazma  - pass ile boş fonksiyon bırakma  - Tüm fonksiyonlar gerçekten çalışır olmalı    Eğer test dosyası yazıyorsan:  {"filename": "test_dosya_adi.py", "code": "...test kodu..."}  """═══════════════════════════════════════════════════════════                    TEST SENARYOSU═══════════════════════════════════════════════════════════Tüm değişiklikler bittikten sonra sistemi başlat ve şunu gir:  "Bir klasördeki tüm CSV dosyalarını okuyup birleştiren   ve özet istatistik çıkaran Python scripti yaz, testleri ile birlikte"BAŞARILI AKIM şöyle görünmeli:  
 PlannerAgent: Plan generated: 4 tasks  
 ResearcherAgent: pandas CSV araştırması
  
 CoderAgent: csv_merger.py yazıldı → workspace/projects/.../src/  
 CoderAgent: test_csv_merger.py yazıldı → workspace/projects/.../tests/  
 CriticAgent: score 7+ → EXECUTOR  
 ExecutorAgent: testler çalıştı, sonuç raporlandı  
 Dosya workspace'e kaydedildiBAŞARISIZLIK DURUMUNDA:  Hata mesajının tamamını kopyala ve paylaş.  Hangi agent'ta patladığını belirt.  workspace/projects/ klasörünün içeriğini listele:    dir workspace\projects\ (Windows)    ls workspace/projects/ (Mac/Linux)═══════════════════════════════════════════════════════════                    ÖNEMLİ NOTLAR═══════════════════════════════════════════════════════════1. Hiçbir dosyayı silme, sadece güncelle2. Her adımdan sonra "Adım X tamamlandı" yaz3. Planner düzeltmesi en kritik adım — dikkatli yap4. AWS_ACCESS_KEY_ID uyarısını görmezden gel, Groq kullanıyoruz5. $0.000000 maliyet görünmesi normal — Groq ücretsiz tierBaşla. Adım 1'den itibaren sırasıyla ilerle.